import Sidebar from "@/components/Sidebar";
import React from "react";

const PetManagement = () => {
  return (
    <div>
      <Sidebar />
      PetManagement
    </div>
  );
};

export default PetManagement;
